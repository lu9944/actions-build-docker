name: Build Dockerfile and Save as TAR

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      dockerfile_path:
        description: 'Dockerfile 路径（相对于仓库根目录，例如：Dockerfile 或 ./docker/Dockerfile）'
        required: true
        default: 'examples/simple-docker/Dockerfile'
      image_name:
        description: '构建后的镜像名称和标签（例如：myapp:latest）'
        required: true
        default: 'myapp:latest'
      build_context:
        description: '构建上下文路径（默认为 Dockerfile 所在目录）'
        required: false
        default: 'examples/simple-docker'
      enable_release:
        description: '是否上传到 GitHub Release'
        required: false
        default: false
        type: boolean
      enable_ftp:
        description: '是否启用 FTP 上传'
        required: false
        default: true
        type: boolean
      ftp_port:
        description: 'FTP 端口（默认：21）'
        required: false
        default: '21'
      ftp_path:
        description: 'FTP 目标路径（例如：/docker-images/）'
        required: false
        default: '/'

jobs:
  build-and-save:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Display Build Configuration
        run: |
          echo "=========================================="
          echo "Dockerfile Builder"
          echo "=========================================="
          echo "Dockerfile 路径: ${{ github.event.inputs.dockerfile_path }}"
          echo "镜像名称: ${{ github.event.inputs.image_name }}"
          echo "构建上下文: ${{ github.event.inputs.build_context }}"
          echo "=========================================="

      - name: Verify Dockerfile Exists
        run: |
          if [ ! -f "${{ github.event.inputs.dockerfile_path }}" ]; then
            echo "❌ 错误: Dockerfile 文件不存在于 ${{ github.event.inputs.dockerfile_path }}"
            echo "请检查路径是否正确"
            exit 1
          fi
          echo "✅ Dockerfile 文件存在"
          echo ""
          echo "Dockerfile 内容预览:"
          echo "=========================================="
          cat "${{ github.event.inputs.dockerfile_path }}"
          echo "=========================================="

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure Docker Data Directory to /mnt
        run: |
          echo "=========================================="
          echo "配置 Docker 数据目录到 /mnt（解决根分区空间不足问题）"
          echo "=========================================="
          # 停止 Docker daemon
          sudo systemctl stop docker docker.socket containerd
          # 创建 /mnt/docker 目录
          sudo mkdir -p /mnt/docker
          # 备份现有 Docker 数据
          if [ -d /var/lib/docker ] && [ ! -L /var/lib/docker ]; then
            echo "移动现有 Docker 数据到 /mnt/docker..."
            sudo mv /var/lib/docker/* /mnt/docker/ 2>/dev/null || true
          fi
          # 配置 Docker daemon 使用 /mnt/docker
          echo '{"data-root": "/mnt/docker"}' | sudo tee /etc/docker/daemon.json
          # 重新启动 Docker
          sudo systemctl start docker
          # 验证配置
          echo "Docker 数据根目录:"
          docker info | grep "Docker Root Dir"
          echo "磁盘使用情况:"
          df -h /mnt/docker
          echo "=========================================="

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME || 'dockerhub_token_user' }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Display Authentication Status
        run: |
          if [ -n "$DOCKERHUB_TOKEN" ]; then
            echo "✅ 已使用 Docker Hub Token 认证（Pro 模式）"
          else
            echo "⚠️  未配置 Docker Hub Token，使用匿名模式（有限制）"
          fi
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Check Disk Space Before Build
        run: |
          echo "=========================================="
          echo "构建前磁盘空间检查"
          echo "=========================================="
          df -h
          echo ""
          echo "Docker 磁盘使用情况:"
          docker system df
          echo "=========================================="

      - name: Build Docker Image
        run: |
          set -e  # 确保任何错误都停止执行

          echo "=========================================="
          echo "开始构建 Docker 镜像"
          echo "=========================================="
          echo "镜像名称: ${{ github.event.inputs.image_name }}"
          echo "构建上下文: ${{ github.event.inputs.build_context }}"
          echo "Dockerfile: ${{ github.event.inputs.dockerfile_path }}"

          # 进入构建上下文目录
          cd "${{ github.event.inputs.build_context }}"

          # 计算相对路径
          BUILD_CONTEXT="${{ github.event.inputs.build_context }}"
          DOCKERFILE="${{ github.event.inputs.dockerfile_path }}"

          # 如果 Dockerfile 在构建上下文内部，使用相对路径
          if [[ "$DOCKERFILE" == "$BUILD_CONTEXT"* ]]; then
            # 移除构建上下文前缀，得到相对于构建上下文的路径
            RELATIVE_DOCKERFILE="${DOCKERFILE#$BUILD_CONTEXT/}"
            echo "使用相对 Dockerfile 路径: $RELATIVE_DOCKERFILE"
            echo "当前目录: $(pwd)"
            echo "Dockerfile 存在检查: $(test -f "$RELATIVE_DOCKERFILE" && echo '存在' || echo '不存在')"
            echo ""
            echo "开始构建..."
            echo "=========================================="

            docker build \
              -f "$RELATIVE_DOCKERFILE" \
              -t "${{ github.event.inputs.image_name }}" \
              . || {
                echo "=========================================="
                echo "❌ 构建失败！"
                echo "请检查 Dockerfile 内容和构建日志"
                exit 1
              }
          else
            # Dockerfile 在构建上下文外部，使用绝对路径
            echo "使用绝对 Dockerfile 路径: $DOCKERFILE"
            echo ""
            echo "开始构建..."
            echo "=========================================="

            docker build \
              -f "../$DOCKERFILE" \
              -t "${{ github.event.inputs.image_name }}" \
              . || {
                echo "=========================================="
                echo "❌ 构建失败！"
                echo "请检查 Dockerfile 内容和构建日志"
                exit 1
              }
          fi

          echo ""
          echo "=========================================="
          echo "✅ 构建成功！"
          echo "=========================================="

      - name: Display Image Details
        run: |
          echo "构建完成的镜像信息:"
          docker images ${{ github.event.inputs.image_name }}
          echo ""
          echo "镜像详细信息:"
          docker inspect ${{ github.event.inputs.image_name }} --format='{{.Id}}'
          docker inspect ${{ github.event.inputs.image_name }} --format='构建时间: {{.Created}}'
          docker inspect ${{ github.event.inputs.image_name }} --format='镜像大小: {{.Size}}'

      - name: Clean Docker Cache Before Save
        run: |
          echo "=========================================="
          echo "清理 Docker 系统以释放空间（保留构建好的镜像）"
          echo "=========================================="
          # 清理构建缓存
          docker builder prune -af
          # 只清理悬挂镜像（dangling），不删除所有未使用的镜像
          docker image prune -f
          # 清理未使用的容器、网络（不清理卷，避免影响数据）
          docker system prune -f
          # 设置 /mnt 目录权限（允许当前用户写入）
          sudo mkdir -p /mnt
          sudo chmod 777 /mnt
          echo "清理完成后的磁盘空间:"
          df -h
          echo "=========================================="

      - name: Save Image as TAR
        run: |
          # 验证镜像是否存在
          echo "检查镜像是否存在: ${{ github.event.inputs.image_name }}"
          if ! docker inspect ${{ github.event.inputs.image_name }} >/dev/null 2>&1; then
            echo "❌ 错误: 镜像 ${{ github.event.inputs.image_name }} 不存在"
            echo ""
            echo "可用的镜像列表:"
            docker images --format "table {{.Repository}}:{{.Tag}}\t{{.Size}}\t{{.CreatedAt}}"
            echo ""
            echo "请检查构建步骤是否成功完成"
            exit 1
          fi
          echo "✅ 镜像存在，继续导出"

          # 将镜像名称中的 / 和 : 替换为下划线，作为文件名
          SAFE_NAME=$(echo "${{ github.event.inputs.image_name }}" | sed 's/[\/:]/_/g')
          # 保存到 /mnt 目录（GitHub Actions 额外磁盘空间，约 14GB）
          IMAGE_FILE="/mnt/${SAFE_NAME}.tar"
          echo "IMAGE_FILE=${IMAGE_FILE}" >> $GITHUB_ENV
          echo "ARTIFACT_NAME=docker-image-${SAFE_NAME}" >> $GITHUB_ENV
          echo "正在保存镜像为: ${IMAGE_FILE}"

          # 显示各个挂载点的磁盘空间
          echo "=== 磁盘空间信息 ==="
          df -h | grep -E "(/mnt|Filesystem|/dev/)"
          echo "=================="

          # 显示镜像大小信息
          IMAGE_SIZE=$(docker inspect ${{ github.event.inputs.image_name }} --format='{{.Size}}' | awk '{print int($1/1024/1024/1024)}')
          echo "镜像大小: ${IMAGE_SIZE}GB"
          echo "根分区可用: $(df -h / | awk 'NR==2 {print $4}')"
          echo "/mnt 分区可用: $(df -h /mnt | awk 'NR==2 {print $4}')"

          # 检查 /mnt 目录的可用空间
          AVAILABLE_SPACE=$(df -BG /mnt | awk 'NR==2 {print $4}' | sed 's/G//')
          echo "/mnt 可用空间: ${AVAILABLE_SPACE}GB"

          # 计算所需空间（镜像大小 + 2GB 缓冲）
          REQUIRED_SPACE=$((IMAGE_SIZE + 2))
          echo "预估需要空间: ${REQUIRED_SPACE}GB"

          if [ "$AVAILABLE_SPACE" -lt "$REQUIRED_SPACE" ]; then
            echo "❌ 错误: /mnt 磁盘空间不足"
            echo "需要: ${REQUIRED_SPACE}GB, 可用: ${AVAILABLE_SPACE}GB"
            echo "建议: 镜像太大，即使使用 /mnt 目录也不够"
            exit 1
          fi

          echo ""
          echo "开始导出镜像（使用管道直接写入 /mnt，避免临时文件占用根分区）..."
          # 使用管道方式保存，避免在 /var/lib/docker/tmp 创建临时文件
          docker save ${{ github.event.inputs.image_name }} > ${IMAGE_FILE}

      - name: Display TAR File Size
        run: |
          echo "TAR 文件信息:"
          ls -lh ${{ env.IMAGE_FILE }}
          echo "文件大小详情:"
          du -h ${{ env.IMAGE_FILE }}
          echo ""
          echo "文件信息:"
          file ${{ env.IMAGE_FILE }}

      - name: Generate Release Info
        if: ${{ github.event.inputs.enable_release == 'true' }}
        run: |
          # 生成时间戳作为 Release tag
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          echo "RELEASE_TAG=docker-build-${TIMESTAMP}" >> $GITHUB_ENV
          echo "TIMESTAMP=${TIMESTAMP}" >> $GITHUB_ENV

      - name: Create Release and Upload TAR
        if: ${{ github.event.inputs.enable_release == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: "${{ env.ARTIFACT_NAME }} - ${{ env.TIMESTAMP }}"
          body: |
            ## Docker 镜像构建导出

            - **镜像名称**: `${{ github.event.inputs.image_name }}`
            - **Dockerfile 路径**: `${{ github.event.inputs.dockerfile_path }}`
            - **构建时间**: ${{ env.TIMESTAMP }}
            - **文件大小**: ${{ env.IMAGE_FILE }}

            ### 使用方法

            ```bash
            # 下载后加载镜像
            docker load -i ${{ env.IMAGE_FILE }}

            # 运行镜像
            docker run -it ${{ github.event.inputs.image_name }}
            ```

            ### 构建信息
            - 此镜像从 Dockerfile 构建而来
            - 可在离线环境中使用
          files: ${{ env.IMAGE_FILE }}
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload to FTP
        if: ${{ github.event.inputs.enable_ftp == 'true' }}
        run: |
          echo "=========================================="
          echo "开始上传到 FTP 服务器"
          echo "=========================================="
          echo "FTP 服务器: *** (已配置)"
          echo "FTP 端口: ${{ github.event.inputs.ftp_port }}"
          echo "FTP 路径: ${{ github.event.inputs.ftp_path }}"
          echo "文件: ${{ env.IMAGE_FILE }}"

          # 确保路径以 / 结尾
          FTP_PATH="${{ github.event.inputs.ftp_path }}"
          if [[ "${FTP_PATH: -1}" != "/" ]]; then
            FTP_PATH="${FTP_PATH}/"
          fi

          # 使用 curl 上传文件到 FTP
          curl -T "${{ env.IMAGE_FILE }}" \
            "ftp://${{ secrets.FTP_SERVER }}:${{ github.event.inputs.ftp_port }}${FTP_PATH}${{ env.IMAGE_FILE }}" \
            --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            --ftp-create-dirs \
            -v

          echo "=========================================="
          echo "✅ FTP 上传完成!"
          echo "=========================================="

      - name: Summary
        run: |
          echo "=========================================="
          echo "✅ 构建任务完成!"
          echo "=========================================="
          echo "镜像名称: ${{ github.event.inputs.image_name }}"
          echo "镜像已保存为: ${{ env.IMAGE_FILE }}"
          echo "Dockerfile 路径: ${{ github.event.inputs.dockerfile_path }}"

          # 根据 Release 开关状态显示不同信息
          if [[ "${{ github.event.inputs.enable_release }}" == "true" ]]; then
            echo "✅ 已上传到 GitHub Release"
            echo "Release Tag: ${{ env.RELEASE_TAG }}"
            echo "请在下方的 Releases 区域下载文件"
          else
            echo "⏭️  已跳过 GitHub Release 上传"
          fi

          # 根据 FTP 开关状态显示不同信息
          if [[ "${{ github.event.inputs.enable_ftp }}" == "true" ]]; then
            echo "✅ 已上传到 FTP 服务器"
          else
            echo "⏭️  已跳过 FTP 上传"
          fi

          echo "=========================================="
          echo ""
          echo "📦 离线使用方法:"
          echo "1. 下载 TAR 文件"
          echo "2. 运行: docker load -i ${{ env.IMAGE_FILE }}"
          echo "3. 运行: docker run -it ${{ github.event.inputs.image_name }}"
          echo "=========================================="
